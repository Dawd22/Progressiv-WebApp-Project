import { TimetoDatePipe } from './timeto-date.pipe';

describe('TimetoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimetoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
